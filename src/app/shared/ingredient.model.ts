export class Ingredient {
   /*  public name: string;
    public amount: number;

    constructor(name: string, amount: number){
        this.name = name;
        this.amount = amount;
    } */

    /* Typescript supports the following structure which
    eventually converted into the code written above */
    constructor(public name: string, public amount: number){        
    }

}